# 复制为 .env 后按需修改。这里的变量会覆盖 config.yaml 同名键。

# --- MongoDB ---
MONGO_URI=127.0.0.1:27017
MONGO_USER=panda
MONGO_PASSWORD=panda
MONGO_AUTH_DB=admin
MONGO_DB=panda
MONGO_TYPE=replica_set
MONGO_REPLICA_SET=rs0

# --- 数据源与凭据 ---
DATAHUBSOURCE=tushare
TS_TOKEN=your_tushare_token
MUSER=
MPASSWORD=
XT_TOKEN=

# --- 定时任务 ---
STOCKS_UPDATE_TIME=20:00
FACTOR_UPDATE_TIME=20:30

# --- LLM（OpenAI/DeepSeek 兼容）---
LLM_API_KEY=your_api_key
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1

# --- 其他 ---
LOG_LEVEL=INFO

# --- API 访问控制与 CORS ---
# 设置后启用简单的 API Key 校验（请求需携带头 X-API-Key）
API_KEY=
# 逗号分隔的允许源，留空或 * 表示放开（仅开发）
CORS_ALLOW_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
